"""Diagnostics use cases shared by the legacy web panel and the desktop.

``report`` builds the live health snapshot; ``actions`` performs the OS
permission / firewall repairs the report's hints point at. Neither module
imports the GUI or the sidecar composition root.
"""
