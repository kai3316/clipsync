"""ClipSync version — the single source of truth.

Bump the version here only. Everything else derives from this module:
pyproject.toml (dynamic), the native About dialog, and the web About panel.
"""

__version__ = "1.0.5"
